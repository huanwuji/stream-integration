package kafka;

message KafkaDataList {
    required int64 sequenceNr = 1;
    repeated KafkaData kafkaData = 2;
}

message KafkaData {
    required string topic = 2;
    required bytes key = 3;
    required bytes message = 4;
}